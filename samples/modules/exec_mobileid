#  Execute external mobileid-radius program
#
#  The RADIUS attributes from the user request will be placed into environment variables of the executed program.
#  For Windows the RADIUS attributes must be passed over the command line as this seems not to be the case.

exec mobileid {
    program = /opt/freeradius/exec-mobileid.sh
    wait = yes
    input_pairs = request
    output_pairs = reply
    shell_escape = yes
    timeout = 120
}